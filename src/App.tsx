import React from 'react';
import ReactDOM from 'react-dom';
import { NAIButton } from './components';

const App = () => {
  return (
    <div>
      <NAIButton />
    </div>
  );
};

ReactDOM.render(<App />, document.getElementById('root'));